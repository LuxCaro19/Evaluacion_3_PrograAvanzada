@model Evaluacion_THA.Models.asignar
@{
    ViewBag.Title = "editar";
    Layout = "~/Views/Shared/_Interna.cshtml";
}

<div class="card">
    <div class="card-header">
        <h5 class="text-center">Edicion de asignacion de herramientas</h5>
    </div>
    <div class="card-body">
        <h5 class="card-title">Asinga una herramienta a un trabajador</h5>
        @using (Html.BeginForm("editar", "Asignar", FormMethod.Post))
        {
            @Html.Partial("_formularios", Model)
        }
    </div>
</div>


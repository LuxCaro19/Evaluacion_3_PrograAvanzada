@model Evaluacion_THA.Models.herramienta
@{
    ViewBag.Title = "crear";
    Layout = "~/Views/Shared/_Interna.cshtml";
}
<br />
<div class="card">
    <div class="card-header">
        <h5 class="text-center">Registro de herramientas</h5>
    </div>
    <div class="card-body">
        <h5 class="card-title">Ingresa una herramienta al sistema</h5>
        @using (Html.BeginForm("crear", "herramienta", FormMethod.Post))
        {
            @Html.Partial("_formularios", Model)
        }
    </div>
</div>

